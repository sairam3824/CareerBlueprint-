version: '3.8'

services:
  jobbot:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    env_file:
      - .env
    environment:
      - FLASK_DEBUG=false
    restart: unless-stopped
